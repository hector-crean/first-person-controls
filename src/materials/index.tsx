import lineMaterial from './lineMaterial'
import outlineMaterial from './outlineMaterial'
import {
	placeholderMaterialProps,
	placeHolderMaterial,
} from './placeholderMaterial'
import stencilMaterial from './stencilMaterial'
import { glassMaterialProps } from './glassMaterial'

export {
	lineMaterial,
	outlineMaterial,
	placeHolderMaterial,
	placeholderMaterialProps,
	stencilMaterial,
	glassMaterialProps,
}

// export const wallMaterial = woodFloorMaterial(DouglasNatural, clippingplanes);
